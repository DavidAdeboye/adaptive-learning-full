version: "3.8"
services:
  app:
    build: .
    ports:
      - "8000:8000"
      - "8501:8501"
    environment:
      - OLLAMA_HOST=http://host.docker.internal:11434
      - OLLAMA_MODEL=llama3.2:1b
      - OLLAMA_TEMPERATURE=0.0
      - GEMINI_API_KEY=
    volumes:
      - ./:/app
